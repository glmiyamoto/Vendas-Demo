<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@android:color/black"
    android:paddingBottom="@dimen/nav_header_padding"
    android:paddingLeft="@dimen/nav_header_padding"
    android:paddingRight="@dimen/nav_header_padding"
    android:paddingTop="@dimen/nav_header_padding"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"
    android:orientation="vertical"
    android:gravity="bottom">

    <ImageView
        style="@style/AppTheme.UserPhoto"
        android:id="@+id/iv_nav_user_photo"
        android:layout_width="@dimen/nav_header_photo_size"
        android:layout_height="@dimen/nav_header_photo_size"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:src="@drawable/photo01" />

    <TextView
        android:id="@+id/tv_nav_user_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:text="Android Studio"
        android:textSize="@dimen/nav_header_user_name_size"
        android:textStyle="normal|bold" />

    <TextView
        android:id="@+id/tv_nav_user_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="android.studio@android.com"
        android:textSize="@dimen/nav_header_user_email_size" />

</LinearLayout>
